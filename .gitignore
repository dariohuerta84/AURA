# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules/

/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# build / dist (Next y el visor Vite)
/build
dist/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
gcp-sa-key-base64.txt

# --- Python / servicio GPU ---
__pycache__/
*.py[cod]
.venv/
venv/
.venv-*/

# TripoSR se clona aparte dentro de gpu_service/ (ver HANDOFF.md)
/gpu_service/TripoSR/

# artefactos generados por el worker GPU: no van a git
/gpu_service/gpu_service_output/

# --- Django muerto (eliminado, no volver a commitear) ---
/backend/

# artefactos de prueba del worker GPU (mock_avatar.glb SI se versiona)
/gpu_service/test_server_output.glb
/gpu_service/valid_avatar.glb
