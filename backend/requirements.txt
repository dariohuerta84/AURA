# Backend del avatar. NO incluye TripoSR: ese se instala aparte en su propio
# repo/entorno (ver README.md), porque arrastra torch y ~1.4GB de pesos.
Django>=5.0,<7.0
djangorestframework>=3.15
django-cors-headers>=4.3
Pillow>=10.0
